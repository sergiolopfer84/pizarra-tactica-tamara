.claude/
.agents/
*.zip
# Registros de firebase-tools y de los emuladores (firebase-debug.log,
# firestore-debug.log, ui-debug.log...). Se regeneran en cada ejecucion.
*-debug.log
# Copias de seguridad de las pizarras: contienen datos reales de jugadores
# (nombres, notas, fotos). El repositorio es público: nunca deben subirse.
backups/
# Claves de acceso en claro que usa copia-pizarras.ps1
claves.txt

.firebase/
